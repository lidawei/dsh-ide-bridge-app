# Peers are satisfied by the linked harness devDependencies and by dsh at runtime.
auto-install-peers=false
# Local harness links live in ./harness; do not pin machine-specific checkouts in the lockfile.
exclude-links-from-lockfile=true
